const fs     = require('fs');
const rcs    = require('../');
const path   = require('path');
const expect = require('chai').expect;

describe('gulp-rcs', () => {
    it('should rename files', done => {
        done();
    });
});
